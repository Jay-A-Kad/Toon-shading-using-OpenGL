TO RUN THIS CODE

--The main folder contains--
--Makefile
--Project Report
--cel_shaded_sphere.cpp
--GLM package
--GL package
--Result.png(this is an image of the expected output)


to run the code-->

got to main folder >> open terminal >> RUN "make">> this will create ./cel_shaded_sphere >> execute this ./cel_shaded_sphere in the terminal
